# requirements.txt

Flask>=2.0            # pour le serveur web et le templating (render_template, session, etc.)
requests>=2.28        # pour tous les appels HTTP vers l’API Spotify
pydantic>=1.10        # pour la validation et la sérialisation des modèles
